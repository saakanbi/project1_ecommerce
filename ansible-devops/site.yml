---
- name: Configure Jenkins server
  hosts: jenkins_server
  become: true
  roles:
    - jenkins

- name: Configure Spring Boot App server
  hosts: app_server
  become: true
  roles:
    - springboot
